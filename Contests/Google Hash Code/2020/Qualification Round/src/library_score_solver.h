#ifndef LIBRARY_SCORE_SOLVER_H
#define LIBRARY_SCORE_SOLVER_H

#include "solution.h"

Solution libraryScoreSolver(vector<Library*> libraries);
Solution slowLibraryScoreSolver(vector<Library*> libraries);

#endif
